## Old School AJAX ASP.NET Core App

This solution uses [HTMX](https://htmx.org) to layer AJAX calls into the UI of an existing web page.

Each request must return a partial response that is replaced inline.

Give it a try.